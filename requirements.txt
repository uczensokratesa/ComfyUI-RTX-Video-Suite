# --- Core dependencies ---
torch>=2.0.0
torchvision>=0.15.0
# Dodajemy torchaudio, bo RTXAudioMuxer go wymaga do zapisu tensorów
torchaudio>=2.0.0 
numpy>=1.24.0
opencv-python>=4.8.0

# --- Audio/Video Processing ---
# Wymagane przez RTXAudioMuxer do łączenia strumieni (musi być w systemie)
# (FFmpeg nie instaluje się przez pip, ale warto o nim wspomnieć w README)

# --- NVIDIA RTX Video Super Resolution SDK ---
# nvvfx musi być zainstalowany oddzielnie z NVIDIA MAXINE SDK
# Link: https://github.com/NVIDIA/MAXINE-VFX-SDK

# --- UI & CLI Enhancements ---
rich>=13.0.0
aiohttp  # Wymagane przez RTXVideoReview.py do obsługi API/serwera
